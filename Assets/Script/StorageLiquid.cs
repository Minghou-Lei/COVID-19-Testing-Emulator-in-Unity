using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class StorageLiquid : MonoBehaviour
{
    public string liquidName;
    public float fill;
    // Start is called before the first frame update
    void Start()
    {
        liquidName = "";
        fill = 0f;
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
